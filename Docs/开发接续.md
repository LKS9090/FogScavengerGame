# 开发接续 · 2026-09-10

## 下次直接开始
用户要求：预制体练习本次收尾，下次直接开始 GPU 运算与官方训练链验证。不要重复预制体教学，不提前安装或锁定训练依赖。以 Docs/项目排期-游戏开发与训练.md v2.1 为执行排期：56 周估算、共同参与、游戏与训练交替推进，旧 A/B 与 48 周安排不再执行。排期中的“未开始”是历史状态，不能覆盖实际成果。

## 当前成果
- Unity 2022.3.62f3c1、URP 14.0.12，工程 C:\Users\kanshan\FogScavengerGame。
- 用户已确认人物移动、转向、墙体碰撞、绿色人物材质和 Windows 独立运行正常。
- 场景 Assets/Scenes/01_PlayerSandbox.unity。
- 本次创建 Assets/Prefabs/Player.prefab，并把场景中的 Player 改为一个预制体实例；保留原人物、FacingMarker、脚本、材质、Character Controller。FacingMarker 的 BoxCollider 原本已禁用，本次保留原设置。
- 验证了预制体 11 个序列化对象、内部引用、场景单个预制体实例与根 Transform 引用。Unity 窗口激活连续两次失败，因此实际采用序列化文件转换，而非成功执行编辑器脚本；本次未重新 Play 或构建验证。下次若 Unity 显示旧场景，应从磁盘重新加载，避免用旧内存场景覆盖已保存的预制体关联。
- GitHub: https://github.com/LKS9090/FogScavengerGame ，main。Library/Builds 等忽略。

## P01 剩余训练工作
1. 复核实际 GPU、驱动、现有解释器与环境；已知 RTX 5080 16 GB，但以当时实际情况为准。
2. 查验 ML-Agents 发布标签、Unity 与 Python/PyTorch 兼容组合。Python 未选定，不把 3.10.12 或旧计划候选当成已锁版本。
3. 建立项目隔离环境，实际 GPU 张量运算和反向传播，记录版本与结果。
4. 官方最小例子短训练、导出并加载；官方模型只验证工具链，不作为游戏策略。
5. 随后继续机器人独立控制、跟随/等待/到点，最小观察/动作/事件记录与连续 10 次无残留重置。

## 教学偏好
用户亲手操作时不主动检查每一步，直接分小步骤教学；用户明确要求代操作时完成实际工作。实际兼容性和结果优先于初步计划。下次不重复询问从哪里开始。

补充验证：随后在临时工程副本中使用同版本 Unity batchmode 实际加载场景，断言通过：恰好一个根级 Player、属于预制体实例、CharacterController 与 PlayerMovement 存在、FacingMarker 存在、位置为 (0,1,0)。已将 Unity 成功加载并重新保存的场景回写到本工程。未重新进行人工 Play 或 Windows 构建，不影响本次预制体加载验证已通过的结论。临时验证脚本未加入正式工程。

Git 同步：本次本地提交已完成，但 GitHub 443 连接超时，推送失败，远端复查同样超时。本地 main 领先 origin/main 一个提交。下次网络恢复后先 git push 同步，然后直接开始 GPU 运算与官方训练链验证。

## 2026-09-11：GPU 与官方工具链依赖已实测解决

用户明确委托自动排查和解决版本依赖问题。新建 Conda fog-mlagents（Python 3.10.12），保留 fog-gpu（Python 3.12）；PyTorch 2.7.1+cu128 在两者均通过 GPU 运算。Release 21 本地源码仅适配 NumPy 依赖至 1.23.5，其他依赖和构建工具固定版本，pip check 通过。

实际通过：mlagents-learn 启动、官方 3DBall 的 Unity 2022.3 Windows 构建、cuda 短训练、从检查点恢复到 16511 步、ONNX 导出及 checker、Sentis CPU 对 100 组固定输入执行并返回有限动作。尚未做正式性能评估、游戏策略训练、异机复现或完整机器人/重置验收。

完整版本、补丁、训练日志与复现命令见 Training/Compatibility/README.md。实验工作目录 C:\Users\kanshan\FogScavengerTraining；已为 VS Code 配置 fog-mlagents 默认解释器，但已有工作区选择可能需要用户通过 Select Interpreter 切换。当前游戏工程不安装 ML-Agents，官方实验项目独立。

GitHub 的旧预制体提交 91d51b7 已在 2026-09-11 成功同步。下次无需再做依赖安装教学，可讲解 3DBall 训练结果/观察动作与模型加载，再继续 P01 机器人跟随/等待及事件与重置工作。

## 2026-09-12：机器人占位对象
用户已报告亲手训练的 my-first-3dball 模型在 Unity 中运行正常，随后委托代做机器人创建步骤。本次通过 Unity 编辑器批处理在 01_PlayerSandbox 中创建根级 Robot 胶囊，Position=(2,0.6,0)、Rotation=(0,0,0)、Scale=(0.6,0.6,0.6)，赋予蓝色 URP Lit 材质 Robot_Mat，保留 CapsuleCollider，没有 PlayerMovement。Unity 保存后重新加载检查通过，临时编辑器脚本已移除。下一步是导航组件与规则跟随/等待；机器人行为尚未实现。

## 2026-09-12：P01 人物与机器人小闭环交付

用户明确改为阶段代开发、由用户集中试玩验收。本次完成规则导航跟随/等待/到点/召回、状态和受阻反馈、边界与目标标记、JSONL 实际事件记录、Python 日志校验，以及独立 Windows 普通构建。最终交付程序自动验收通过，包括实际绕墙、命令替换和十次连续重置；11 个回合起点、125 条记录（以 Reports/P01 中最终 JSON 报告为准）。具体实现、操作、测试局限和维护入口见 Docs/P01交付说明.md。

当前供试玩的程序在 Builds/P01/FogScavenger.exe。Windows 网络权限弹窗需用户手动处理，本测试无需入站网络；没有自动操作系统安全窗口。尚未完成无遮挡窗口中的人工鼠标交互验收或异机复现，下一步先收集用户试玩反馈，再进入 P02 协作原型。当前机器人均为规则导航，没有新增游戏模型训练。
